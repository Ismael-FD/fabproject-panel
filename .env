# NEXT_PUBLIC_API_URL should point to the backend API server.
# If using ngrok, ensure the tunnel is active and the URL is up to date.
NEXT_PUBLIC_API_URL=https://manlessly-unparadoxal-leeann.ngrok-free.dev